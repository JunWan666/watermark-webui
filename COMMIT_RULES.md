# Watermark WebUI 提交信息规范

本仓库所有 Git 提交信息均使用以下格式，确保隐形水印功能、界面、鉴权、数据存储和部署配置的变更可以按顺序追溯。

## 格式

提交信息为单行，格式如下：

```
<序号>-<状态>: <本次大概概括>
```

示例：

```
001-新增: 完成隐形水印嵌入、提取和记录管理功能
002-改动: 优化水印工作区与处理记录的移动端布局
003-修复: 修复初始化页面 Logo 尺寸溢出问题
004-配置: 添加 Docker Compose 与双架构镜像构建配置
005-发布: 发布 v1.0.0 版本
```

## 规则

1. **语言**：必须使用简体中文。
2. **序号**：三位数字，从 `001` 开始递增（001、002、003 …），与提交次数一一对应，不重复、不跳号。
3. **状态**：从下方状态词表中选择一个最贴切的词，放在序号后、冒号前。
4. **概括**：一句话说明本次提交做了什么，尽量写明涉及的水印功能、界面、API、数据存储或部署配置，避免只写「更新」「修改」等空泛描述。
5. **单行**：首行必须符合上述格式；如需补充细节，可空一行后写正文，但正文不参与格式校验。

## 状态词表

| 状态 | 适用场景 |
|---|---|
| 新增 | 添加水印功能、API、页面或新文件 |
| 改动 | 调整已有功能、界面、处理逻辑或代码结构 |
| 修复 | 修复功能、界面、兼容性或部署问题 |
| 发布 | 版本发布、打 tag、更新版本号 |
| 删除 | 移除功能或文件 |
| 文档 | 仅更新 README、部署说明、注释或其他文档 |
| 配置 | 修改 Docker、环境变量、依赖或仓库配置 |
| 测试 | 新增或修改测试 |
| 迁移 | 结构调整、目录迁移、重命名 |

## 为什么

- **中文**：与项目文档和主要使用场景保持一致。
- **序号**：让提交历史可顺序追溯，防止漏看或重复提交。
- **状态词**：让历史可按类型筛选（如 `git log --grep="-修复:"`）。
